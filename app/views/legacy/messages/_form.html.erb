<%= form_tag legacy_message_reply_path(thread, thread.other_user(user)) do  %>
  <dl class="zend_form">
    <dd id="body-element">
    <%= text_area_tag :body, '', :size => "80x3" %>
    </dd>
    <%= submit_tag t('mailboxer.new.button') %>
  </dl>
<% end %>

