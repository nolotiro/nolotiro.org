<% content_for :title , t('conversations.index.title') %>

<%= render partial: 'index',
           locals: { conversations: @conversations, user: current_user },
           layout: 'application/profile' %>
