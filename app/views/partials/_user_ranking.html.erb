<%= render layout: 'partials/ranking',
           locals: { title_key: title_key, ranking: ranking } do %>
  <% ranking.ranked.each do |id, name, n_ads| %>
    <li>
      <%= link_to "#{name} (#{n_ads})", listads_user_path(id) %>
    </li>
  <% end %>
<% end %>
