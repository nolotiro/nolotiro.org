<%= render layout: 'partials/ranking',
           locals: { title_key: title_key, scope: scope } do %>
  <% scope.ranked.each do |id, name, n_ads| %>
    <li>
      <%= link_to "#{name} (#{n_ads})", ads_woeid_path(id, type: "give") %>
    </li>
  <% end %>
<% end %>
