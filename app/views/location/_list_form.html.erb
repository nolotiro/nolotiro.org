<%= form_tag({action: "change"}) do %>
  <fieldset><legend></legend>
    <dl>
      <dt>&nbsp;</dt>
      <dd>
      <%= select_tag(:location, options_for_select(location_asked)) %>
      </dd>
      <dt>&nbsp;</dt>
      <dd>
      <%= submit_tag(t('nlt.change_town.choose')) %>
      </dd>
    </dl>
  </fieldset>
<% end %>
