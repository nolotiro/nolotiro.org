<%= form_tag({action: "list"}) do %>
  <fieldset>
    <legend>
      Por favor, escribe tu ciudad
    </legend>
    <dl class="zend_form">
      <dt id="location-label">
      <label for="location" class="required">
        Ciudad:
      </label>
      </dt>
      <dd id="location-element">
      <%= text_field_tag(:location) %>
      </dd>
      <dt id="submit-label">&nbsp;</dt>
      <dd id="submit-element">
      <%= submit_tag("Enviar") %>
      </dd>
    </dl>
    Nuestro sistema de sugerencia de ubicación (basado en tu IP) dice que estas cerca de:
    <br>
    <b>
      <a href="/es/location/change2/?location=<%= location_suggest %>"><%= location_suggest %></a>
    </b>
  </fieldset>
<% end %>
