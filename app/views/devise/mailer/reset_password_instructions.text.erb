<%= t('nlt.hello_to', :recipient => @resource.email) %>

<%= t('devise.passwords.follow_instructions') %>

<%= link_to t('devise.passwords.action'), edit_password_url(@resource, :reset_password_token => @token) %></p>

<%= edit_password_url(@resource, :reset_password_token => @token) %>
