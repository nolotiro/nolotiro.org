<% content_for :meta_title, t('nlt.user_edit_profile') %>

<div id="default_header_section">
  <h1><%= t('nlt.user_edit_profile') %></h1>
</div>

<div class="col-md-9 col-sm-9">
  <div class="row">
    <%= render "users/tabnav", user: @user %>

    <div class="ad_excerpt">
      <%= render 'edit_form' %>
    </div>
  </div>
</div>
